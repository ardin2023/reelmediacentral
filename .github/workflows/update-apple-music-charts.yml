name: Update Apple Music Charts

on:
  schedule:
    - cron: "0 11 * * 1"  # Every Monday at 11:00 AM UTC
  workflow_dispatch:       # Allow manual runs from the Actions tab

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Fetch Apple Music Charts
        run: python frontend/scripts/fetchAppleMusicCharts.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add frontend/public/data/music-charts-applemusic.json
          git diff --cached --quiet || git commit -m "chore: update Apple Music Charts [skip ci]"
          git push
